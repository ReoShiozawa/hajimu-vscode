# はじむ VS Code 拡張 検証チェックリスト

## ビルド

- [ ] `npm run compile` が成功する
- [ ] `npm run verify:assets` が成功する
- [ ] `snippets/hajimu.json` がJSONとして読み込める
- [ ] 拡張機能起動時に `はじむ (Hajimu) 拡張機能 v2.0.2` のログが出る

## 入力補助

- [ ] `kansu` / `hensuu` / `moshi` / `modosu` で日本語キーワード補完が出る
- [ ] `function` / `return` / `print` で日本語キーワード補完が出る
- [ ] `fn ` / `fnr ` / `cl ` / `if ` / `lp ` / `wh ` / `tc ` がテンプレート展開される
- [ ] `imp ` / `gui ` / `web ` / `bot ` / `render ` / `audio ` / `rpg ` がimportテンプレートへ展開される
- [ ] コメント・文字列内でローマ字展開が暴発しない

## 診断とQuick Fix

- [ ] `終わり` 不足に、開始行つきの説明とQuick Fixが出る
- [ ] `もし 条件` に `なら` 追加Quick Fixが出る
- [ ] 未定義識別子に近い候補への置換Quick Fixが出る
- [ ] `GUI.ボタン()` など未取り込みプラグイン利用にimport候補が出る
- [ ] `表示("text"` や `辞書["キー"` に閉じ括弧Quick Fixが出る
- [ ] `返す` と `戻す` が混在した時に `戻す` への統一Quick Fixが出る

## ホバーと学習支援

- [ ] `関数` / `もし` / `戻す` / `表示` のホバーに英語圏用語が出る
- [ ] `はじむ: 選択コードを説明` がMarkdownを生成する
- [ ] `はじむ: このコードを改善` が外部AIなしで改善メモを生成する
- [ ] `はじむ: この構文を英語圏言語の用語で説明` がPython/JavaScript例を出す
- [ ] `はじむ: エラーが出ている行を説明` が現在行の診断を説明する
- [ ] `はじむ: サンプルから開始` で基本 / GUI / Web / Discord / ゲームを選べる

## プラグイン補完

- [ ] `GUI.` / `ウェブ.` / `ボット.` の後に関数補完が出る
- [ ] `描画.` / `物理2D.` / `RPG.` / `音声.` / `エンジン.` の後にKaname向け関数補完が出る
- [ ] `描画["` の後にブラケット形式の関数補完が出る
- [ ] `描画["ウィンドウ作成"]` などにホバーが出る
- [ ] `取り込む "` の中でプラグイン名と推奨エイリアスの補完が出る

## 実コード確認

- [ ] `test_all_features.jp` を開いて意図しない大量警告が出ない
- [ ] Kaname の `src/runtime/デプロイ.jp` で `engine_*` の補完とimport案内が自然に出る
- [ ] jp-discord の `examples/*.jp` で `ボット.` 補完が自然に出る
- [ ] jp-pack のエンジン例でドット形式・ブラケット形式の補完が機能する
